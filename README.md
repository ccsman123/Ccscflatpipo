# Pipotron CCSC (Flat version)

Une app Next.js sans aucun dossier, utilisant `index.tsx` directement à la racine.  
Parfaite pour un déploiement rapide sur Vercel depuis mobile.

## Démarrage local

```
npm install
npm run dev
```
